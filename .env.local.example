# Copy to .env.local and fill in values.
# For local dev, run `npx supabase status -o env` to print the local stack's
# URL and keys.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
